const BackgroundColor = '#141923';
const ForegroundColor = '#324364';

const ButtonBackgroundColor = rgba(254, 254, 254, 0.03);
const ButtonForegroundColor = rgba(255, 255, 255, 0.56);

const StrokeColor = rgba(114, 127, 151, 0.46);

function rgba(r: number, g: number, b: number, a: number) {
    return `rgba(${r}, ${g}, ${b}, ${a})`;
}
